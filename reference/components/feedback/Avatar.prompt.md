Circular avatar with image or initials-on-gradient fallback.

```jsx
<Avatar name="Amara Okafor" />
<Avatar src="/p.jpg" name="Amara Okafor" size="lg" ring />
```

Props: `src`, `name` (drives initials + tooltip), `size` (`sm`–`xl`), `ring`.
